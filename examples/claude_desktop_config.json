{
  "mcpServers": {
    "memory-for-ai": {
      "command": "uv",
      "args": ["run", "--directory", "path/to/memory/for/ai", "memory-server"],
      "env": {
        "MEMORY_SERVER_LOG_LEVEL": "INFO"
      }
    }
  }
}